#include "Struct.hpp"
////////////////////////////////////////////////////////
//Random - input is a seed, output is a "randomnumber"//
////////////////////////////////////////////////////////
#include <stdint.h>
uint32_t Random(unsigned long int userseed,bool first_start);

//////////////////////////////////////////////////////////////////////////
//RandomMapParts - create the information for the Random Area of the map//
//////////////////////////////////////////////////////////////////////////
void RandomMapParts(int tempmapparts[],MapOptions Options);

////////////////////////////////////////////////////
//GetBiomeType - gives back a Biometype or nothing//
////////////////////////////////////////////////////
int GetBiomeType(int tempmapparts[],int patches_x, int patches_y, int pos_x, int pos_y);

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//SetRandomOptions - This will for now only set my default optionsm Maybe later I will also add a way to change them//
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
MapOptions SetRandomOptions(MapOptions Options);

////////////////////////////
//Adds all the information//
////////////////////////////
MapOptions SetInformation(MapOptions Options,int mapwidth,int mapheight);